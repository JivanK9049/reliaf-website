import { useState } from "react";
import { FaChevronDown } from "react-icons/fa";
const questions = [["Can freshers apply?", "Yes. Freshers may apply for suitable entry-level opportunities and training-focused roles."], ["What is the interview process?", "After an initial resume review, shortlisted candidates are invited for a conversation with the relevant team."], ["What about salary?", "Compensation is based on the role, experience, and skills, with performance incentives for eligible positions."], ["Which locations do you hire for?", "Our openings are mainly across Maharashtra. Each position card lists its current location." ]];
export default function FAQ() { const [open, setOpen] = useState(0); return <section className="px-6 py-24"><div className="mx-auto max-w-3xl"><div className="text-center" data-aos="fade-up"><p className="font-bold tracking-widest text-[#15803d]">FAQ</p><h2 className="mt-3 text-4xl font-black text-slate-800">Questions, answered.</h2></div><div className="mt-12 space-y-3">{questions.map(([question, answer], i) => <div key={question} className="overflow-hidden rounded-2xl border border-green-100"><button onClick={() => setOpen(open === i ? -1 : i)} className="flex w-full items-center justify-between gap-5 bg-white px-6 py-5 text-left font-bold text-slate-800"><span>{question}</span><FaChevronDown className={`shrink-0 text-[#15803d] transition ${open === i ? "rotate-180" : ""}`} /></button><div className={`grid transition-all duration-300 ${open === i ? "grid-rows-[1fr]" : "grid-rows-[0fr]"}`}><p className="overflow-hidden px-6 text-slate-600"><span className="block border-t border-green-50 py-5">{answer}</span></p></div></div>)}</div></div></section>; }
